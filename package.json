{
  "name": "create-for",
  "version": "0.8.10",
  "description": "`npm init for <repo-name>` base code",
  "repository": "https://github.com/allenhwkim/create-for.git",
  "scripts": {
    "build": "rimraf dist && webpack"
  },
  "engines": {
    "node": ">=7"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.com/"
  },
  "bin": {
    "create-for": "index.js"
  },
  "files": [
    "index.js"
  ],
  "dependencies": {
    "fs-extra": "^6.0.1",
    "inquirer": "^6.0.0",
    "mustache": "^2.3.0",
    "node-fetch": "^2.1.2"
  },
  "devDependencies": {}
}
